# {{feature_name}} — Design

> Generated by SpecForge on {{date}} via `sf-propose`.
> Workflow: {{workflow_type}}
> Status: pending approval

## Architecture

{{architecture_overview}}
<!-- How this feature fits into the existing system.
     Include a component diagram description or ASCII diagram. -->

## Components

### {{component_1_name}}

- **Responsibility:** {{what_it_does}}
- **Interface:** {{inputs_outputs}}
- **Dependencies:** {{what_it_needs}}
- **Location:** {{file_path_or_module}}

### {{component_2_name}}

- **Responsibility:** {{what_it_does}}
- **Interface:** {{inputs_outputs}}
- **Dependencies:** {{what_it_needs}}
- **Location:** {{file_path_or_module}}

<!-- Continue for each component -->

## Data Model

{{data_model_description}}
<!-- Entities, schemas, state shape. Use code blocks for schemas:

```python
@dataclass
class Entity:
    id: str
    name: str
    created_at: datetime
```
-->

## API / Interface Contracts

{{interface_contracts}}
<!-- Endpoints, CLI commands, function signatures, event schemas.

```
POST /api/v1/resource
  Request:  { "name": string, "type": enum }
  Response: { "id": string, "created_at": string }
  Errors:   400 (validation), 409 (conflict), 500 (internal)
```
-->

## Sequence / Flow

{{sequence_description}}
<!-- Describe the main flow step by step:
1. User triggers X
2. Component A validates input
3. Component A calls Component B
4. Component B writes to database
5. Response returned to user
-->

## Technical Decisions

| Decision | Choice | Rationale | Alternatives Considered |
|----------|--------|-----------|------------------------|
| {{decision_1}} | {{choice_1}} | {{rationale_1}} | {{alternatives_1}} |
| {{decision_2}} | {{choice_2}} | {{rationale_2}} | {{alternatives_2}} |

<!-- If a decision required deep research, a full analysis lives in
     specforge/features/<name>/decisions/<decision-name>.md
     See sf-propose/references/research.md -->

## Error Handling

{{error_handling_strategy}}
<!-- Strategy for failures, retries, user-facing errors.
     Map to requirements: which R# covers which error scenario. -->

## Testing Strategy

| Level | What | How |
|-------|------|-----|
| Unit | {{unit_target}} | {{unit_approach}} |
| Integration | {{integration_target}} | {{integration_approach}} |
| E2E | {{e2e_target}} | {{e2e_approach}} |

<!-- Optional sections — include ONLY when relevant; OMIT the heading entirely
     otherwise. Do not write "N/A". No ceremony without purpose.

  ## Security Considerations
  Include only if the feature has security-sensitive surfaces (auth, untrusted
  input, secrets, data protection). {{security_notes}}

  ## Performance Considerations
  Include only if the feature has performance-sensitive paths (hot loops, large
  data, latency targets, caching). {{performance_notes}}
-->
