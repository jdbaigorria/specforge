# {{feature_name}} — Tasks

> Generated by SpecForge on {{date}} via `sf-propose`.
> Status: pending approval

## Tasks

A **flat** list of tasks. Tasks declare their dependencies (`depends_on`) — they
do NOT declare waves. Wave grouping is an execution concern that `sf plan compute`
derives from the dependency graph at build time (the WHAT lives here; the
HOW-it's-grouped lives in the build plan).

- [ ] **T1:** {{task_description}} → {{requirement_refs}}
  - **Expected:** {{what_exists_after_this_task}}
  - **Files:** {{files_created_or_modified}}
  - **Tests:** {{test_files_to_create}}
  - **Depends on:** — (no dependencies)

- [ ] **T2:** {{task_description}} → {{requirement_refs}}
  - **Expected:** {{what_exists_after_this_task}}
  - **Files:** {{files_created_or_modified}}
  - **Tests:** {{test_files_to_create}}
  - **Depends on:** T1

<!-- Continue with more tasks as needed. Guidelines:
     - Every task MUST trace to at least one R#
     - Every R# MUST be covered by at least one task
     - Declare `depends_on` (task IDs) for any task that needs another's output.
       Be honest and minimal — the dependency graph drives the wave layout, so a
       missing edge means a task could be scheduled too early, and a spurious one
       serializes work that could run in parallel.
     - Do NOT group into waves here — `sf plan compute` does that.
-->

## Structured form (`tasks.json`)

The CLI source of truth is `tasks.json` — a flat array, each task with
`depends_on`:

```json
{
  "feature": "{{feature_name}}",
  "tasks": [
    { "id": "T1", "title": "...", "requirement_refs": ["R1"], "depends_on": [] },
    { "id": "T2", "title": "...", "requirement_refs": ["R2"], "depends_on": ["T1"] }
  ]
}
```

## Traceability Matrix

| Requirement | Task(s)     | Acceptance Criteria |
|-------------|-------------|---------------------|
| R1          | T1          | AC1.1, AC1.2        |
| R2          | T2          | AC2.1               |

## Validation Checklist

Before approving this task plan, verify:

- [ ] Every requirement (R#) maps to at least one task
- [ ] Every task maps to at least one requirement
- [ ] `depends_on` edges are correct (no circular, no missing, no spurious)
- [ ] Each task has clear expected output and file list
- [ ] Test files are identified for each task
