{
  "schema_version": "1.0",
  "feature": "slugify",
  "summary_md": "A single pure function in `src/texttools/slug.py`, re-exported from `__init__.py`. Standard library only (`unicodedata` for accent folding, `re` for tokenizing).\n\n**Algorithm:** NFKD-normalize and drop combining marks (folds accents, R2); lowercase; find runs of `[a-z0-9]+` with a regex (drops punctuation, R3); join the runs with `sep` (single separators, R1/R3); the result is `\"\"` when there are no runs (R4).\n\n**Public API:** `def slugify(text: str, sep: str = \"-\") -\u003e str`.",
  "components": [
    {
      "id": "C1",
      "name": "slugify function",
      "kind": "function",
      "responsibilities": [
        "normalize + fold accents",
        "tokenize alnum runs",
        "join with separator",
        "return empty on no runs"
      ],
      "depends_on": []
    }
  ],
  "decisions": [
    {
      "id": "D1",
      "question": "Should the separator be configurable?",
      "options": [
        {
          "name": "Hard-code \"-\"",
          "pros": [
            "simplest"
          ],
          "cons": [
            "callers wanting \"_\" must post-process"
          ]
        },
        {
          "name": "Optional sep param defaulting to \"-\"",
          "pros": [
            "flexible",
            "no required arg"
          ],
          "cons": [
            "one extra parameter"
          ]
        }
      ],
      "chosen": "Optional sep param defaulting to \"-\"",
      "rationale_md": "Not required by any requirement, defaults to `-`; kept minimal per \"no ceremony without purpose\"."
    }
  ],
  "interfaces": [],
  "sections_included": [
    "components",
    "decisions"
  ]
}
