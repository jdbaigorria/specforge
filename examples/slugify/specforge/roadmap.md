# Roadmap — texttools

| Feature | Status | Notes |
|---------|--------|-------|
| slugify | done | URL-safe slugs from arbitrary text |
| truncate | planned | Length-limited text with ellipsis, word-boundary aware |
| normalize | planned | Whitespace + unicode normalization |

Serial flow: one active feature at a time. `truncate` starts after `slugify` is
archived.
