# Project: tempconv

**Stack:** Python 3.10+, standard library only. Tests with pytest.
**Architecture:** a single `convert` module of pure functions.
**Status:** brownfield — `c_to_f` existed before SpecForge was added.
